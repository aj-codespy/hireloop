GEMINI_API_KEY=''
SCORING_MODEL='gemini-2.5-flash'
STT_MODEL='gemini-2.5-flash'
TTS_MODEL='gemini-2.5-flash-preview-tts'
TTS_VOICE_EN='Kore'
TTS_VOICE_HI='Kore'
PORT='8001'
INTERVIEW_OVERALL_LIMIT_SECONDS='600'
INTERVIEW_RECONNECT_HOURS='2'

# Optional: faster STT (recommended for production)
# DEEPGRAM_API_KEY=''

# Supabase — required for token-gated interviews + report persistence
SUPABASE_URL=''
SUPABASE_SECRET_KEY=''

# Email (Resend) — interview expired notifications
RESEND_API_KEY=''
RESEND_FROM=''
APP_URL='http://localhost:3000'

# Shared secret for admin→API calls (pre-render question TTS). Must match web .env.local.
INTERVIEW_INTERNAL_SECRET=''

# Legacy Live model (no longer used by structured interview mode)
MODEL='gemini-3.1-flash-live-preview'
